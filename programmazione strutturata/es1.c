/*Scrivere un programma in C che converte un numero da
decimale a esadecimale e ottale.*/
#include <stdio.h>

int main()
{
    unsigned int a = 127;
    printf("Numero in base 10: %d, in base 16: %x, in base 8: %o", a, a, a);
}